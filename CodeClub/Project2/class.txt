git help
git status = Mostra qual é o status do repositorio 
clear = limpa o terminal 

git config --global user.name "seu nome"
git config --global user.email "seu email"

git inti = novo repositorio na pasta onde estou 
git add = add arquivos para serem comitados 
git add . = add todos os arquivos de uma vez só 

